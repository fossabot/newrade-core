import { Buttons } from '@newrade/core-design-system';
import { defaultButtons } from '@newrade/core-react-ui-theme';

export const buttons: Buttons = {
  ...defaultButtons,
  //
  // vars: reference to colorVars
};
